<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.REORDER_TASKS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name=".core.EduLockApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EduLock"
        android:hardwareAccelerated="true"
        android:windowSoftInputMode="adjustNothing"
        android:largeHeap="true"
        android:requestLegacyExternalStorage="true"
        android:preserveLegacyExternalStorage="true"
        tools:targetApi="31">

        <!-- ACTIVITIES -->
        <activity
            android:name=".ui.acitvity.TimeLimitActivity"
            android:exported="false" />
        <activity
            android:name=".ui.acitvity.statistics_usage_data"
            android:exported="false"
            android:theme="@style/Theme.EduLock.NoActionBar" />
        <activity
            android:name=".ui.acitvity.login_register"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"/>
        <activity
            android:name=".ui.acitvity.register_signup"
            android:windowSoftInputMode="adjustResize"
            android:exported="false" />
        <activity
            android:name=".ui.acitvity.all_done_activity"
            android:exported="false" />
        <activity
            android:name=".ui.acitvity.settingsGrantPermission"
            android:exported="false" />
        <activity
            android:name=".ui.acitvity.termsAndAgreement"
            android:exported="false" />
        <activity
            android:name=".ui.acitvity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.acitvity.OverlayBlockedActivity"
            android:theme="@style/Theme.AppCompat.Dialog"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:exported="false"
            android:showOnLockScreen="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustNothing" />

        <!-- SERVICES FOR MONITORING AND BLOCKING -->
        <service
            android:name=".service.BlockOverlayService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:permission="android.permission.BIND_FOREGROUND_SERVICE"
            android:stopWithTask="false"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </service>

        <service
            android:name=".service.AppMonitoringService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:permission="android.permission.BIND_FOREGROUND_SERVICE"
            android:process=":monitor"
            android:stopWithTask="false"
            android:isolatedProcess="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </service>

        <!-- USAGE MONITOR SERVICE - Sends notifications at 1h, 2h, 3h, etc -->
        <service
            android:name=".service.UsageMonitorService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:stopWithTask="false" />

        <!-- ACCESSIBILITY SERVICE - For app blocking -->
        <service
            android:name=".service.AppBlockerAccessibilityService"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- BROADCAST RECEIVERS -->

        <!-- Restart service when app monitoring crashes -->
        <receiver android:name=".service.AppMonitoringService$ServiceRestartReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="com.example.edulock.RESTART_SERVICE" />
            </intent-filter>
        </receiver>

        <!-- Watch if service stops -->
        <receiver
            android:name=".receiver.ServiceWatcherReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.edulock.CHECK_SERVICE" />
            </intent-filter>
        </receiver>

        <!-- Reset usage stats at midnight -->
        <receiver
            android:name=".receiver.UsageStatsResetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.edulock.RESET_STATS" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- DAILY SUMMARY - Sends notification at midnight with total screen time -->
        <receiver
            android:name=".receiver.DailySummaryReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.edulock.DAILY_SUMMARY" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Boot receiver - Restarts UsageMonitorService when phone restarts -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

    </application>

</manifest>